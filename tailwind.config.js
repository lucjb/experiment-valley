/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './*.html',    // Match all HTML files in root
    './js/*.js'    // Match all JS files in js directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}